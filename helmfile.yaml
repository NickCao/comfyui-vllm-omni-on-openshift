releases:
  - name: vllm-omni-z-image-turbo
    namespace: comfyui-vllm-omni
    chart: ./charts/vllm-omni
    values:
      - values/vllm-omni.yaml

  - name: vllm-qwen3-06b
    namespace: comfyui-vllm-omni
    chart: ./charts/vllm-omni
    values:
      - values/vllm-qwen3-06b.yaml

  - name: portal
    namespace: comfyui-vllm-omni
    chart: ./charts/portal
    values:
      - values/portal.yaml

  - name: litellm
    namespace: comfyui-vllm-omni
    chart: ./charts/litellm
    values:
      - values/litellm.yaml
