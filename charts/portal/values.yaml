image:
  repository: registry.example.com/comfyui-portal
  tag: latest
  pullPolicy: Always

replicas: 1

# GitHub OAuth2 credentials.
github:
  clientID: ""
  clientSecret: ""
  callbackURL: ""

# Session secret for express-session.
sessionSecret: ""

# Comma-separated list of GitHub usernames allowed to use the portal.
# Must be set — empty string means no users are allowed.
allowedGithubUsers: ""

# Helm configuration for managing ComfyUI instances.
helm:
  chartPath: /app/charts/comfyui
  releasePrefix: comfyui-

# OpenShift Route.
route:
  enabled: true
  host: ""
  tls:
    termination: edge

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi
